use super::{scalar::Scalar, vector::Vector};

pub type Mass = Scalar;

pub type Acceleration = Vector;
pub type Force = Vector;
pub type Position = Vector;
pub type Velocity = Vector;

pub type Distance = Vector;
